package test;

import static org.junit.Assert.assertTrue;
import static org.junit.jupiter.api.Assertions.assertEquals;

import controller.classes.ManagerImpl;

public class DirectorTest {
	
	@org.junit.Test
	public void DirectorTest1() {
		
	}
	
	//ACCETTO RICHIESTA DUE DIRETTORI
 /*
  * ManagerImpl.getManager().satisfiesRequestDirector(terzaRichiesta, "PrimoDir");
			assertTrue(ManagerImpl.getManager().showDirectorInfo("PrimoDir").getRequestsToSatisfy().isEmpty());
			assertEquals(ManagerImpl.getManager().showDirectorInfo("PrimoDir").getAcceptedRequest(), terzaRichiesta);
  */
}
